package Java8.FunctionalInterface;

public interface FunctionaInterface {
    //abstract void single();
    //A funtional interface is interface having only 1 abstract method but can have number of
    // default and static methods.
    //Example - Runnable, Callable, Comparable, Comparator

    //void m1();

    void sum(int a,int b);
}
