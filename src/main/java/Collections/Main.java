package Collections;

import java.util.Collection;
import java.util.HashMap;
import java.util.Map;

public class Main {
    public static void main(String[] args) {
        //Difference between ArrayList and vector is that vectors have access to only single thread while arraylist
        //has access to multiple threads making arrayList faster and more reliable


        /*Set not always stores distinct values
        * if we store primitive values, they are stored distinctly,
        * but if we store any custom object or wrapper class, so in order to store only distinct values
        * we have to override hashCode and equals method in the class, else they will store duplicate values
        * as well*/

        
    }
}
